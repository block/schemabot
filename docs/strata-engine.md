# Strata (experimental)

Block currently uses Strata for sharded MySQL. The integration is experimental
and is not yet available for open-source use. We plan to make it available in
the future; there is no public setup path or release date yet.

Use the supported database integrations for new open-source deployments.
