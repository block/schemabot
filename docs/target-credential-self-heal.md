# Target Credential Probe and Self-Heal

<!-- BEGIN TOC (auto-generated by `make docs-toc`) -->

## Table of Contents

- [Current behavior](#current-behavior)
- [Decision: probe each configured target once at startup](#decision-probe-each-configured-target-once-at-startup)
- [Decision: layer a DSN-keyed cache with evict-and-retry-once](#decision-layer-a-dsn-keyed-cache-with-evict-and-retry-once)
- [Decision: classify typed authentication failures centrally](#decision-classify-typed-authentication-failures-centrally)
- [Observability](#observability)
- [Engine scope](#engine-scope)
- [Rollout](#rollout)
- [Non-goals](#non-goals)

<!-- END TOC (auto-generated by `make docs-toc`) -->

Target credentials are a data-plane dependency. Today SchemaBot validates a
target only when a routed operation first opens a connection. A malformed
secret, a missing grant, or a dead host can therefore remain invisible from
deploy time until the first pull, plan, or apply reaches that target.

This design adds two layers with different jobs:

- a warn-only startup probe gives deploy-time confidence without making target
  availability a fleet-availability dependency; and
- use-time cache replacement and one bounded retry repair credentials at the
  point where correctness requires them.

The startup probe is advisory because validation cannot remove the time-of-check
to time-of-use window. An external secret can refresh after `SELECT 1` and before
the next session authenticates. Use-time self-heal is therefore the correctness
mechanism, not a fallback for a supposedly authoritative probe.

## Current behavior

In gRPC mode, the control plane sends an opaque target to the data plane. The
data plane's `pkg/tern/target_router.go` `TargetRouter.clientForTarget` asks the
configured inventory to resolve that target, then caches a namespace-bound
`LocalClient`. `TargetRouter.Health` pings SchemaBot storage only. Likewise,
`pkg/tern/local_client.go` `LocalClient.Health` calls `storage.Ping`; neither
health path connects to a target database.

The cache key contains target, database type, environment, and namespace, but
not the resolved DSN. It has no eviction policy. The `DSNFrom` comment in
`pkg/inventory/static.go` records the consequence: `dsn_from` assembles a fresh
DSN on every request, but `clientForTarget` discards it on a cache hit. Rotation
is effective only at the next cache miss.

MySQL target paths open connections with `mysqlconn.Open` and then
`PingContext`, for example `LocalClient.pullSchemaNamespace` in
`pkg/tern/local_client.go` and `Engine.fetchCurrentSchema` in
`pkg/engine/spirit/spirit.go`. PostgreSQL plan and pull do the equivalent with
`postgresconn.Open` in `pkg/engine/postgres/postgres.go` `Engine.Plan` and
`pkg/engine/postgres/pull.go` `Engine.PullSchema`. These opens use the DSN
captured by the cached `LocalClient`.

The storage pool already has a separate credential-rotation mechanism:
`mysqlconn.OpenReloadable` and `postgresconn.OpenReloadable`, wired by
`pkg/serve/serve.go`, retry new storage connections after typed authentication
failures. Target pools deliberately use `Open`, so that mechanism does not
self-heal target credentials.

In current deployments, MySQL targets use static `dsn:` entries. The static
resolver resolves those once when it is built, and the credential rotation
runbook requires a process reboot. This design does not change that `dsn`
semantic. PostgreSQL is the first target fleet using per-request `dsn_from`.

## Decision: probe each configured target once at startup

The data plane will issue `SELECT 1` once for every configured target when its
server starts. The probe resolves the target through the same inventory path as
a request and opens it through `mysqlconn.Open` or `postgresconn.Open`, including
the normal transport policy. It verifies authentication, database selection,
and enough grant to execute a read without inspecting or changing schema.

Each probe has its own context timeout. A fixed-size goroutine pool bounds
concurrency, so target count cannot create an unbounded connection burst and a
slow target occupies at most one slot until its timeout. Completion, refusal,
resolution failure, and timeout each produce one metric observation and one
structured log entry. DSNs, passwords, and raw secret values are never logged.

The exact lifecycle hook is `pkg/serve/serve.go` `Server.Start`, alongside the
other background startup work and after `Build` has constructed the shared
`TargetRouter`. The probe runs asynchronously from that hook. `serve.Run`
registers and starts the gRPC listener before calling `Server.Start`, and the
HTTP listener follows independently; probe completion is not a precondition of
either listener.

The probe has **zero effect on startup, readiness, or liveness**:

- it never returns an error from `Server.Start`;
- `/livez` remains process-only; and
- `/health` and Tern `Health` continue to reflect storage connectivity, not
  target connectivity.

One target can be intentionally offline, isolated to one environment, or in a
maintenance window while every other target remains usable. Making that target
fail readiness would remove a data-plane process that could serve all healthy
targets. Making it fail liveness would crash-loop the whole fleet without
repairing the database or secret. The warn and metric expose the defect while
preserving useful capacity.

The probe runs at startup only in the first version. A periodic sweep would
duplicate request-time detection, maintain idle target connections, and create
continuous load proportional to inventory size. If operators later need
credential-expiry forecasting, that should be designed as a separately paced
monitor with explicit cardinality and load budgets.

**Rejected alternatives.** Probing during `Build` was rejected because it would
gate process construction and delay listeners. Reusing `TargetRouter.Health` was
rejected because one unhealthy target would make the whole shared data plane
unready. Treating a successful probe as permission to skip use-time recovery was
rejected because secret refresh makes that conclusion stale immediately.

## Decision: layer a DSN-keyed cache with evict-and-retry-once

DSN-keyed caching and failure-triggered retry solve different races, so the
design uses both.

First, `TargetRouter.clientForTarget` will include a one-way hash of the fully
resolved DSN in its cache identity. The raw DSN must not become a map key that
can appear in diagnostics. For `dsn_from`, the next request after a secret
refresh resolves a different DSN, misses deterministically, and constructs a
fresh `LocalClient` without waiting for an authentication failure.

The logical route still owns at most one current client. Publishing a client
for a new DSN hash atomically replaces and closes the prior client for that
target, type, environment, and namespace. A map that retains every historical
hash would leak pools after every rotation, so stale generations are evicted on
replacement. Concurrent creators keep the existing duplicate-close behavior:
one generation wins and the unused client is closed.

Second, an authentication-classified failure before an operation starts
mutating causes the router to evict the exact failed generation, resolve the
target again, and retry once. Compare-and-delete semantics prevent a request
that failed on an old client from evicting a newer generation installed by a
peer. The retry re-runs resolution rather than reusing the failed DSN. One retry
bounds pressure when the secret backend and database disagree permanently.

The two layers cover the time-of-check to time-of-use sequence:

```text
┌─────────┐   ┌──────────────────┐   ┌───────────────────────┐   ┌────────┐
│ request │──▶│ resolve dsn_from │──▶│ cache lookup: DSN hash│──▶│ client │
└─────────┘   └──────────────────┘   └───────────────────────┘   └───┬────┘
                                                                    │
                                                           auth error?
                                                      no ───────────┴──▶ success
                                                                    │ yes
                                                                    ▼
                                                             ┌──────────┐
                                                             │ classify │
                                                             └────┬─────┘
                                                                  │ auth
                                                                  ▼
                                              ┌────────────────────────────┐
                                              │ evict exact generation     │
                                              │ re-resolve + retry once    │
                                              └─────────────┬──────────────┘
                                                            │
                                              ┌─────────────┴──────────────┐
                                              ▼                            ▼
                                         ┌─────────┐                ┌───────────────┐
                                         │ success │                │ typed failure │
                                         └─────────┘                └───────────────┘
```

Retry safety is determined at the routed operation boundary, not merely by the
error code:

| Routed operation | Automatic retry | Reason |
| --- | --- | --- |
| `PullSchema` | Yes | Reads catalog state; no target mutation has begun. |
| `Plan` | Yes | Live-schema reads and plan persistence are idempotent before target execution. |
| `PlanDiff` | Yes | Read-only and non-persisting. |
| Initial client construction or connection probe | Yes | No operation has begun. |
| `Apply` | No | Materialization and execution share the call boundary; an error does not prove target mutation never started. |
| Resume, cutover, stop, cancel, revert, skip-revert | No | They act on existing work; replay belongs to their durable, operation-specific reconciliation paths. |

An implementation may retry a pre-apply connection acquisition only if it is
factored before the engine's apply call and can prove no statement, deploy
request, or durable apply transition has started. The router must not retry the
whole `Apply` method. Database integrity takes priority over hiding a credential
failure.

Static `dsn:` targets benefit from failure eviction only if their resolver can
return updated configuration, for example after a future config reload. Under
today's static resolver, the DSN is resolved once, so eviction reconstructs the
same credentials and the retry returns a typed failure. The credential rotation
runbook's reboot step remains necessary for static-`dsn:` MySQL targets until
they move to `dsn_from`.

**Rejected alternatives.** DSN-keying alone was rejected because a credential
can rotate between resolution and connection, leaving the newly selected client
stale before first use. Evict-and-retry alone was rejected because every normal
rotation would require a failed request even though `dsn_from` already supplied
the new value. Unbounded retries were rejected because permanent grant errors
would amplify traffic and secret resolution. Retrying apply was rejected because
an authentication-looking failure is not evidence that no mutation preceded it.

## Decision: classify typed authentication failures centrally

A shared, dialect-aware classifier returns a small closed enum:

| Classification | MySQL driver code | PostgreSQL SQLSTATE | Meaning |
| --- | --- | --- | --- |
| `AuthInvalidCredentials` | 1045 | `28P01` | User/password rejected. |
| `AuthNoGrant` | 1044 | `28000` | Authorization or database access rejected. |
| `AuthNoDatabase` | 1049 | — | Selected database does not exist. |
| `NotAuth` | everything else | everything else | Preserve the existing failure path. |

Classification uses `errors.As` against `*mysql.MySQLError` from the Go MySQL
driver and `*pgconn.PgError` from pgx. It never matches error strings. Wrapping
therefore preserves classification while redaction and contextual error text
remain independent.

`AuthNoDatabase` and `AuthNoGrant` trigger the same single repair attempt as
invalid credentials because rotation can change the selected database, role, or
grant together with the password. If re-resolution returns the same bad shape,
the bounded retry fails without a loop. The enum remains distinct so operators
can distinguish secret mismatch from provisioning and configuration errors.

Network refusal, DNS failure, context cancellation, timeout, TLS negotiation or
certificate failure, protocol errors, query errors, and all other SQLSTATE or
MySQL codes are `NotAuth`. They do not evict a valid credential generation and
continue through the operation's existing error handling.

**Rejected alternatives.** String matching was rejected because driver wording
and wrapping change, localized server messages are not stable, and messages can
contain secret or endpoint data. A boolean classifier was rejected because it
would erase the low-cardinality outcome needed for useful logs and metrics.

## Observability

Metric names follow the repository's OpenTelemetry convention: a
`schemabot.<area>.<event>_total` counter with bounded attributes, and every
sample carries `environment` (using `unknown` only when no target environment
exists).

| Metric | Attributes | Meaning |
| --- | --- | --- |
| `schemabot.target.probe_total` | `target`, `database_type`, `environment`, `outcome` | One startup result per configured target. Outcomes: `success`, `auth_invalid_credentials`, `auth_no_grant`, `auth_no_database`, `resolve_error`, `timeout`, `connection_error`. |
| `schemabot.target.client_evictions_total` | `database_type`, `environment`, `reason` | Replaced generations. Reasons: `dsn_changed`, `auth_invalid_credentials`, `auth_no_grant`, `auth_no_database`. Target is omitted from this hot-path counter to bound cardinality. |
| `schemabot.target.auth_retries_total` | `operation`, `database_type`, `environment`, `classification`, `outcome` | Eligible single retries and whether they succeeded, failed, or could not re-resolve. |

The target label is acceptable on the startup probe because configured
inventory is bounded and each target emits once per process start. Retry and
eviction can occur on a hot path, so their counters omit target and rely on logs
for attribution. The retry counter earns a separate instrument because a rate
or storm is an actionable signal that rotation or provisioning is unhealthy.

Every probe log includes `target`, `database_type`, `environment`, `outcome`, and
`duration_ms`; failures also include `classification` and `error`. Every
self-heal log includes those routing fields plus `namespace`, `operation`,
`classification`, `attempt`, `old_dsn_hash`, `new_dsn_hash`, and `outcome`.
Hashes are short correlation identifiers, never reversible credentials. Raw
DSNs and secret values are forbidden. Apply-scoped logs continue to use the
existing `LogAttrs()` helpers and add only these call-specific fields.

**Rejected alternative.** Logging without counters was rejected for startup
coverage and retry storms: both need a fleet-wide denominator or rate. A metric
attribute containing namespace, raw DSN, error text, or secret reference was
rejected for cardinality and disclosure risk.

## Engine scope

The probe, classifier, DSN-generation cache, and safe retries cover both MySQL
and PostgreSQL targets. MySQL uses the Go MySQL driver error type and
`mysqlconn.Open`; PostgreSQL uses pgx's error type and `postgresconn.Open`.

SchemaBot also has a Vitess engine. Its target inventory can carry API metadata
and, optionally, a vtgate DSN, but the built-in PlanetScale path is not a direct
MySQL/PostgreSQL target session. Vitess is excluded from this first version:
`SELECT 1` would validate at most vtgate while most operations authenticate to
the PlanetScale API. Existing Vitess client/API error handling remains in force.
A later Vitess probe must define which credential and endpoint are authoritative
before joining this shared outcome model.

## Rollout

1. Add the shared typed classifier and exhaustive tests for wrapped MySQL and
   PostgreSQL driver errors, including every `NotAuth` class.
2. Add the bounded, asynchronous startup probe to `Server.Start`, warn-only,
   with logs and `schemabot.target.probe_total`.
3. Add compare-and-delete eviction and one retry to read-only routed operations;
   leave apply and control methods on their existing failure paths.
4. Add the DSN-hash generation to `clientForTarget`, atomically replacing and
   closing stale clients so cache size stays bounded per logical route.
5. Run a credential-rotation drill on the PostgreSQL pilot. Retire the runbook's
   reboot step for `dsn_from` targets after the metrics prove deterministic
   replacement and bounded retry; retain it for static-`dsn:` MySQL targets
   until those targets move to `dsn_from`.

## Non-goals

- Gating startup, readiness, or liveness on target health.
- Periodically probing every target in the first version.
- Managing, generating, rotating, or distributing secrets and database grants.
- Changing `dsn` resolution or reload semantics.
- Retrying apply, control operations, or any operation after target mutation may
  have begun.
- Making Vitess API or vtgate credentials part of the first implementation.
